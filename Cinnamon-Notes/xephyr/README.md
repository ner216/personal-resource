# Xephyr

Xephyr window is a "nested" X server that allows you to run a regular application
window inside your current session. For example, you can run s second
instance of the Cinnnamon desktop within it so that a crash does not
freeze the actual desktop.
