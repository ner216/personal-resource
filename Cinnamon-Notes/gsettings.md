# gsettings

The Gsettings for Cinnamon are defined in the `data/org.cinnamon.gschema.xml` file.
Each setting contains a declaration and default setting. All setting options are defined in
the python settings GUI at `files/usr/share/cinnamon/cinnamon-settings/`.
