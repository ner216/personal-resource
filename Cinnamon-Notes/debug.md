# Debug Tips

### Javascript level errors:
- Can be viewed normally via Looking Glass inside of Cinnamon session:
    - Access Looking Glass using the `alt+F2` launcher, then use `lg`.
- If the desktop is frozen or not accepting input:
    - View logs in the `~/.xsession-errors` file from a TTY.
